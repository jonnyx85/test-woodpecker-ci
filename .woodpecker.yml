when:
  - event: [push, pull_request]

steps:
  - name: test
    image: node:20-alpine
    commands:
      - npm install
      - npm test
      
  - name: build
    image: node:20-alpine
    commands:
      - npm run build
      
  - name: docker-build
    image: plugins/docker
    settings:
      repo: jonkai/test-ci-app
      tags: latest
      dockerfile: Dockerfile
    when:
      event: push
      branch: main

  - name: notify-discord-success
    image: appleboy/drone-discord
    settings:
      webhook_id: 1402426366166700163
      webhook_token: 2-1obP18tKLS-EKZAUwtEK-UZOP3P7WoWDwi__wHaAUHEaKKfDcI7PHU7BCDGHFCgFfI
      message: |
        ✅ **Build Successful!**
        **Repository:** {{repo.owner}}/{{repo.name}}
        **Branch:** {{commit.branch}}
        **Commit:** {{commit.message}}
        **Author:** {{commit.author}}
        **Build:** #{{build.number}}
        **Status:** Success 🎉
        [View Commit]({{commit.link}})
    when:
      status: success

  - name: notify-discord-failure
    image: appleboy/drone-discord
    settings:
      webhook_id: 1402426366166700163
      webhook_token: 2-1obP18tKLS-EKZAUwtEK-UZOP3P7WoWDwi__wHaAUHEaKKfDcI7PHU7BCDGHFCgFfI
      message: |
        ❌ **Build Failed!**
        **Repository:** {{repo.owner}}/{{repo.name}}
        **Branch:** {{commit.branch}}
        **Commit:** {{commit.message}}
        **Author:** {{commit.author}}
        **Build:** #{{build.number}}
        **Status:** Failed ⚠️
        [View Commit]({{commit.link}})
    when:
      status: failure